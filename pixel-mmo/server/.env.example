ANTHROPIC_API_KEY=sk-ant-...     # optional — for AI worldbuilder (attribute gen + lore check)
OPENAI_API_KEY=sk-...            # optional — for DALL-E pixel art image generation
ADMIN_USERNAME=admin             # default admin login username
ADMIN_PASSWORD=admin1234         # default admin login password (change before deploy!)
JWT_SECRET=dev_secret_change_in_production
PORT=3000
DB_PATH=./db/worldbuilding.db
